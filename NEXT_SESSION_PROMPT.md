Continue the Inferno GPU/Metal project. Repo: `/home/makr/Documents/inferno-vgpu-kext` (GitHub `MakrSas/inferno-vgpu-kext`). QEMU working dir: `/home/makr/Documents/Inferno/InfernoData`.

**Read `/home/makr/Documents/inferno-vgpu-kext/PROJECT_STATUS.md` in full first — it is the single source of truth.** It's very long (~4400+ lines) but has every finding, every playbook, every gotcha. This file was written at the end of a very long, high-progress day — the host machine was being shut down for the day right after this, so treat everything below as accurate as of that moment, but the guest VM itself will need a fresh boot next session (the machine was fully powered off, not just the VM).

**What changed today, roughly in the order it happened (skim `PROJECT_STATUS.md`'s dated sections for full detail on any of these):**
1. Closed this project's long-standing "no `ipsw`" tooling gap for real: built and validated a from-scratch `dyld_shared_cache` parser, `dsc_parse.py` (symbol↔address lookup, export-trie walking), then extended it to parse real Objective-C class/protocol/method metadata too. This is genuinely reusable infrastructure now — reach for it before doing any more blind static analysis of DSC-resident frameworks.
2. Found WidgetKit's real extension-hosting protocol (`chronod` daemon, `HostToExtensionXPCInterface`/`ExtensionToHostXPCInterface`, `getDescriptors`/`getTimeline`/etc.) using that tool — this is *why* the earlier `NSExtensionMain`-shaped widget prototype could never have worked (every widget in this build is WidgetKit, not legacy `NCWidgetProviding` — confirmed build-wide, no exceptions).
3. Investigated whether `CARenderer`/`CARenderServer` (QuartzCore's internal render engine, which backboardd is a thin client of) has a real Metal backend — it does (`kCARendererMetalCommandQueue`, real API surface) — but three separate, increasingly rigorous live-GDB attempts (including one covering `backboardd`+`SpringBoard`'s *entire* startup from the literal first instant of a fresh boot) all found zero evidence it's ever actually used in this build. **Deprioritized as a likely dead end** — the existing "software/`IOSurface`+`IOMobileFramebuffer` compositing only" finding stands.
4. Built a real `main()`-shaped widget-host binary, swapped it into `StocksWidget.appex/StocksWidget` on the guest, and got it live-hosted by WidgetKit's real machinery — it renders as WidgetKit's own standard "Нет доступного контента"/"No content available" placeholder (a clean, non-crashing failure — proves the process launches and gets recognized, just doesn't answer the XPC protocol correctly yet).
5. Found a new, previously-undocumented security gate (**gate #6**: an app-container `file-write-data` denial, distinct from gates #1-#5) needed to even write into a widget's bundle — disassembled, patched, live-verified, **and (last action of the day) permanently baked into `patch_kernelcache.py` + verified surviving a real fresh restart**. Trust it as permanent, same as #1-#5.
6. Found a separate, real problem: overwriting one file inside `Stocks.app`'s bundle breaks the *whole container's* code-signature validity, cascading `Sandbox: hook..execve() killing <unsigned>[pid=N]: attempting to use a container without a code signing identity` kills to sibling/XPC processes of that app. **This only blocks opening the full `Stocks.app` when tapped — it does NOT block the widget's own on-screen rendering**, which is the actual goal. Don't let this become a distraction from the real next step below.
7. Extended `dsc_parse.py` with real ObjC metadata parsing (protocols, classes, method lists — iOS 14's shared cache uses relative/small method lists, confirmed and handled) and used it to get the real Objective-C interface for `HostToExtensionXPCInterface`. Built `inferno_widget_host_xpc.m`, a real XPC responder — it builds in CI and was ad-hoc-signed (`ldid`) to test whether that alone fixes the container-signature-cascade problem for a *freshly swapped-in* binary specifically. **This was mid-live-test (had gotten as far as confirming the XPC interface shape and that `getDescriptors` currently returns an empty array — not yet proven whether `chronod` even connects and calls it at all) when the day ended** — the single most valuable next step is finishing this live test.

**Standing instruction from the user**: keep working continuously without stopping to ask permission at each step; the user has explicitly said "делай что угодно, главное добиться Metal" ("do whatever it takes, the main thing is getting Metal working") — this is real license to make judgment calls on approach, not just execute a fixed plan. Use parallel background subagents liberally for long-running/independent investigations, but be mindful: only ONE thread can have live QEMU/serial/GDB access at a time (this bit multiple agents today — always verify actual guest state directly rather than trusting a stale assumption, and don't hesitate to resume/nudge a background agent that's gone quiet — several today claimed to be "waiting for a background transfer" but had actually gone fully idle; a `SendMessage` resume with a concrete status update reliably got them moving again). Commit and push progress incrementally, not just at the end — several sessions today nearly lost significant work to being cut off mid-task (a session-limit rate-cap once, a planned host shutdown once) with nothing committed yet.

**Open threads, roughly in priority order:**
1. **Finish the WidgetKit XPC responder live test** (item 7 above) — this is the closest-to-done, most direct path to the actual stated goal (Metal-rendered content visible in the real, live, unmodified-compositor interface). Concrete next steps are written into `PROJECT_STATUS.md`'s own final dated section — read it, it has an exact numbered checklist (trace-log check, `dmesg` check, `ps` check, screendump check) for picking this back up.
2. If the XPC responder proves out: this becomes the first real, working "app content rendered via this project's Metal bridge, composited into the live interface by backboardd's completely unmodified logic" milestone — the whole project's stated end goal. Worth its own celebration/writeup, and worth then trying to generalize beyond one hardcoded widget.
3. If it doesn't: the container-signature-cascade problem (item 6 above) becomes load-bearing after all — two documented directions to try (`ldid` + patch `_CodeSignature/CodeResources`'s hash table entry for the swapped file, vs. a 7th kernel gate patch for whatever check enforces container-wide signature validity) — see that section's own detail.
4. Lower priority, not touched today: the `agx_system_metal_test` pre-`main()` dyld crash (has a real, source-grounded hypothesis — dyld3's `ClosureBuilder::recursiveLoadDependents`, an `OverflowSafeArray`/`vm_deallocate` hazard — with a concrete live-verification recipe, never attempted live), the MapKit `/b` sandbox-deny investigation (6 precomputed patches ready, live capture never achieved), a rare one-off `compute_test` kernel panic (reproduced once, not root-caused).

Goal: real Metal rendering for both individual apps and the whole system interface, at 60fps, on this QEMU-emulated iOS 14 kernelcache.
